RSpec.describe Auth0Authorization do
  it "has a version number" do
    expect(Auth0Authorization::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
